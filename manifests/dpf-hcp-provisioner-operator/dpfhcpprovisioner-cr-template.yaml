apiVersion: provisioning.dpu.hcp.io/v1alpha1
kind: DPFHCPProvisioner
metadata:
  name: <HOSTED_CLUSTER_NAME>
  namespace: <CLUSTERS_NAMESPACE>
spec:
  # Base domain for the hosted cluster
  baseDomain: <BASE_DOMAIN>

  # Reference to DPUCluster CR (created by DPF operator)
  dpuClusterRef:
    name: <HOSTED_CLUSTER_NAME>
    namespace: dpf-operator-system

  # Reference to DPUDeployment CR (created by DPF operator)
  dpuDeploymentRef:
    name: dpudeployment
    namespace: dpf-operator-system

  # BlueField ocp image layer
  machineOSURL: <BLUEFIELD_OCP_IMAGE>

  # Storage class for etcd
  etcdStorageClass: <ETCD_STORAGE_CLASS>

  # OCP release image
  ocpReleaseImage: <OCP_RELEASE_IMAGE>

  # Pull secret reference
  pullSecretRef:
    name: <DPFHCPPROVISIONER_PULL_SECRET_NAME>

  # SSH key reference
  sshKeySecretRef:
    name: <DPFHCPPROVISIONER_SSH_SECRET_NAME>

  # Control plane availability policy
  controlPlaneAvailabilityPolicy: <CONTROL_PLANE_POLICY>
